<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Isolation model: the proot container never binds /sdcard, /storage,
         /mnt or /data - only the app-private rootfs, the app-private
         /workspace (filesDir/workspace) plus /dev, /proc, /sys and read-only
         Android system files needed by the bootstrap shell.

         The APP itself requests full file access ("All files access") so its
         own file-manager screen can show ALL device folders and files; device
         files reach the container only through the app-private /workspace. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29"
        tools:ignore="ScopedStorage" />
    <uses-permission
        android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="AllFilesAccess" />

    <application
        android:name=".RedTermApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.RedTermApp"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:name=".ui.WelcomeActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|density|keyboard|keyboardHidden"
            android:theme="@style/Theme.RedTermApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.MainActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|density|keyboard|keyboardHidden"
            android:theme="@style/Theme.RedTermApp" />

        <activity
            android:name=".ui.TerminalActivity"
            android:exported="false"
            android:theme="@style/Theme.RedTermApp"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|density|keyboard|keyboardHidden"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".ui.SettingsActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|density|keyboard|keyboardHidden"
            android:theme="@style/Theme.RedTermApp" />

        <activity
            android:name=".ui.BashTemplatesActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|density|keyboard|keyboardHidden"
            android:theme="@style/Theme.RedTermApp" />

        <activity
            android:name=".ui.FilesActivity"
            android:exported="false"
            android:theme="@style/Theme.RedTermApp"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|density|keyboard|keyboardHidden" />

        <activity
            android:name=".ui.BrowserActivity"
            android:exported="false"
            android:theme="@style/Theme.RedTermApp"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|density|keyboard|keyboardHidden"
            android:windowSoftInputMode="adjustResize" />

        <service
            android:name=".ui.QuickSettingsTile"
            android:exported="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <receiver
            android:name=".ui.NightModeReceiver"
            android:exported="false" />

        <service
            android:name=".service.TerminalService"
            android:foregroundServiceType="specialUse"
            android:exported="false" />

    </application>

</manifest>
